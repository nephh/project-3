/** @type {import('tailwindcss').Config} */
export default {
  content: ["./index.html", "./src/**/*.{js,jsx}"],
  // darkMode: "class", if we want to implement dark mode, uncomment this. It will use all the dark: classes.
  theme: {
    extend: {},
  },
  plugins: [],
};
